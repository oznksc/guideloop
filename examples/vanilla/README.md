# Vanilla HTML

`@guideloop/vanilla` without React.

```bash
npm run build   # repo root
npx serve examples/vanilla
```

Open the served URL and click **Start tour**. Imports `packages/vanilla/dist/esm`.
