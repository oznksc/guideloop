# Tour Builder (lab)

Pick DOM → edit steps → preview → export JSON/TS.

```bash
npm run build   # repo root
cd examples/tour-builder && npm i && npm run dev
```

Also available in-app via `@guideloop/react/builder` (dev-only).
