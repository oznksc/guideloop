# Next.js App Router

Multi-page tour with `"use client"` island + `persist` / `autoRestore`.

```bash
# repo root
npm run build
cd examples/nextjs-app-router && npm i && npm run dev
```

→ http://localhost:3100

Uses `@guideloop/react` + `@guideloop/react/types` for RSC-safe step modules.
