# Templates

Reusable scaffolds extracted from the GuideLoop monorepo.

| Template | Description |
|----------|-------------|
| [`dev-product-landing`](./dev-product-landing) | Developer-product landing shell: vertical rail, themes, install dropdown, stack explorer |

```bash
cd templates/dev-product-landing && npm install && npm run dev
```
