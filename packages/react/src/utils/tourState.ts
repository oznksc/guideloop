// Re-export from core for backward compatibility
export { saveTourState, loadTourState, clearTourState } from '@guideloop/core';
export type { TourState, StorageType, PersistConfig } from '@guideloop/core';
