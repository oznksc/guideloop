// Re-export from core for backward compatibility
export {
  buildSelector,
  isBuilderChrome,
  resolvePickTarget,
  validateSelector,
} from '@guideloop/core';
export type { SelectorStatus } from '@guideloop/core';
